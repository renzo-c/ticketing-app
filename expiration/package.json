{
  "name": "expiration",
  "dependencies": {
    "@rcnp-tickets/common": "^1.0.12",
    "@types/bull": "^4.10.0",
    "bull": "^4.12.0",
    "node-nats-streaming": "^0.3.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "start": "ts-node-dev src/index.ts",
    "test": "jest --watchAll --no-cache"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "./src/test/setup.ts"
    ]
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "29.5.0",
    "ts-jest": "^29.1.1"
  }
}
